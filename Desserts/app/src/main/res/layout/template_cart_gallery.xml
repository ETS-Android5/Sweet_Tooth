<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.fragments.DetailsCakesFragment">

    <ImageView
        android:id="@+id/details_image"
        android:layout_width="55dp"
        android:layout_height="55dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_margin="16dp"
        app:srcCompat="@drawable/cake1_1"
        android:scaleType="centerCrop" />

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="@+id/details_image"
        app:layout_constraintStart_toStartOf="@+id/details_image"
        android:layout_marginStart="-10dp"
        android:layout_marginTop="-10dp"
        android:backgroundTint="@color/burgundy"
        android:background="@drawable/ic_baseline_close_circle"
        android:id="@+id/remove_from_cart"/>

</androidx.constraintlayout.widget.ConstraintLayout>